# ── Local development (gitignored — copy from .env.example) ─────────────────────
# Leave VITE_API_URL empty — Vite proxies /api to the backend.
VITE_API_URL=

# ── Local dev server ───────────────────────────────────────────────────────────
VITE_DEV_PORT=5173
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080

# ── Map routing (optional — defaults to public OSRM) ───────────────────────────
VITE_OSRM_URL=https://router.project-osrm.org/route/v1/driving
